// Localized voice landing — kept out of the index for now (paid/campaign use).
export const metadata = {
  robots: { index: false, follow: true },
};

export default function LocaleLandingLayout({ children }) {
  return children;
}
