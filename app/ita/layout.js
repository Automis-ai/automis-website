// Italian Voice AI landing (voice.automis.ai) — kept out of the index for now.
export const metadata = {
  robots: { index: false, follow: true },
};

export default function LocaleLandingLayout({ children }) {
  return children;
}
